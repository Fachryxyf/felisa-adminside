<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Admin AyudCraft</title>
    <link rel="icon" href="./assets/logo.png" />
    <style>
        body { margin: 0; font-family: 'Poppins', sans-serif; background-color: #f8f9fa; display: flex; }
        .sidebar { width: 250px; background-color: #fbc6a4; height: 100vh; padding: 20px; }
        .sidebar h2 { text-align: center; font-family: 'Playfair Display', serif; }
        .sidebar ul { list-style: none; padding: 0; }
        .sidebar ul li a { display: block; padding: 15px; text-decoration: none; color: #3a3a3a; font-weight: 600; border-radius: 8px; }
        .sidebar ul li a.active, .sidebar ul li a:hover { background-color: #ff8c42; color: white; }
        .main-content { flex-grow: 1; padding: 40px; }
    </style>
</head>
<body>