<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - AyudCraft</title>
    <link rel="icon" href="./assets/logo.png" />
    
    <%# --- TIGA BARIS KODE FONT YANG BENAR --- %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #FDFCFB; /* Latar belakang body yang lebih hangat */
        }

        .login-wrapper {
            display: flex;
            width: 800px;
            height: 550px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border-radius: 20px;
            overflow: hidden;
        }

        .login-form-container {
            flex: 1;
            padding: 50px;
            background: #FFF7F0; /* Latar belakang form yang lebih hangat dan serasi */
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .login-image-container {
            flex: 1;
            background: url('https://cdn.pixabay.com/photo/2023/10/31/23/52/generated-to-8356214_1280.png') no-repeat center center;
            background-size: cover;
        }

        h1 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 10px;
        }

        p {
            color: #777;
            margin-bottom: 30px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            font-size: 0.9rem;
        }

        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            background: #fff;
        }
        
        .options {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
            margin-bottom: 25px;
        }

        .options a {
            color: #ff8c42;
            text-decoration: none;
        }

        .options a:hover {
            text-decoration: underline;
        }

        button.login-btn {
            width: 100%;
            padding: 14px;
            border: none;
            border-radius: 8px;
            background-color: #ff8c42;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button.login-btn:hover {
            background-color: #e07a38;
        }

        .error-message {
            color: #d9534f;
            background-color: #f2dede;
            border: 1px solid #ebccd1;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            font-size: 0.9rem;
        }

        @media (max-width: 850px) {
            .login-wrapper {
                width: 90%;
                max-width: 450px;
                flex-direction: column;
                height: auto;
            }
            .login-image-container {
                display: none; /* Sembunyikan gambar di layar kecil */
            }
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-form-container">
            <h1>Masuk Sekarang</h1>
            <p>Hi, Admin Ayud Craft 👋</p>

            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>

            <form action="/login" method="POST">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required placeholder="email@example.com">
                </div>
                <div class="form-group">
                    <label for="password">Kata Sandi</label>
                    <input type="password" id="password" name="password" required placeholder="********">
                </div>
                <div class="options">
                    <label><input type="checkbox" name="remember"> Ingat saya</label>
                    <a href="/forgot-password">Lupa kata sandi?</a>
                </div>
                <button type="submit" class="login-btn">Masuk</button>
            </form>
        </div>
        <div class="login-image-container"></div>
    </div>
</body>
</html>